export const config = {
  supportedTools: ['rspack', 'vite'],
  // Generated by scripts/generate-case.mjs: the entry (src/index.jsx) never
  // mounts anything to #root -- it only computes a checksum over the route
  // registry so a real build cannot tree-shake it away (see entryBody's own
  // comment in that file) -- so there is nothing for a dev/HMR benchmark to
  // observe here. This case is build-only, matching cases/popular-libs and
  // cases/ui-components, the other two cases in this repo without
  // rootFile/leafFile.
  dev: false,
};
